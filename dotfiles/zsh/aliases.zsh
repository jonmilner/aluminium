alias c="clear"
alias ll="ls -lFh"
alias la="ls -lAFh"  # List all files (inlcuding hidden)

# git

## branch
alias gb="git checkout -b" # branch & checkout
alias gbl="git branch --sort=-committerdate" # list by date

## checkout
alias co="git checkout"
alias gco="git checkout"

## status
alias gd="git diff"
alias gl="git l"
alias gs="git status"

## push/pull
alias gp="git pull"
alias gpu="git push"

# add/commit
alias ga="git add"
alias gcm="git commit -m"

# stash
alias gsp="git stash pop"
alias gst="git stash save -u"
